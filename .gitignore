# macOS
.DS_Store
._*

# Editors
.vscode/
.idea/

# Python
__pycache__/
*.pyc
*.pyo

# Secrets — never commit
.env
.env.*
*.key
*.pem
credentials*.json

# Brand portal regen artifacts (regenerable, not source)
# (kept tracked — they ARE the artifact, mirrors parspec-design policy)

# Vercel build output (regenerated by scripts/build-portal-dist.sh on every deploy)
public/
.vercel/

# npm package dist (regenerated by scripts/build-package.py before publish)
plugins/ek-web/skills/ek-web/package/dist/

# npm pack tarballs
*.tgz

